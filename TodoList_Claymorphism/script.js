let todos=[],filter='all';const input=todoInput,list=todoList;function render(){list.innerHTML='';todos.filter(t=>filter==='all'||(filter==='active'&&!t.done)||(filter==='completed'&&t.done)).forEach((t,i)=>{const li=document.createElement('li');if(t.done)li.classList.add('completed');li.innerHTML=`<div class="left"><input type="checkbox" ${t.done?'checked':''}><span>${t.text}</span></div><button>삭제</button>`;li.querySelector('input').onchange=()=>{t.done=!t.done;render()};li.querySelector('button').onclick=()=>{todos.splice(i,1);render()};list.appendChild(li)})}function add(){if(!input.value.trim())return;todos.push({text:input.value,done:false});input.value='';render()}addBtn.onclick=add;input.onkeydown=e=>e.key==='Enter'&&add();document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelector('.active').classList.remove('active');b.classList.add('active');filter=b.dataset.filter;render()});